<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fun Fact</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        @keyframes slideIn {
            from {
                transform: translateY(-20px);
                opacity: 0;
            }
            to {
                transform: translateY(0);
                opacity: 1;
            }
        }
        .slide-in {
            animation: slideIn 0.8s ease-out;
        }
    </style>
</head>
<body class="bg-gradient-to-r from-blue-200 to-blue-500 flex items-center justify-center min-h-screen">
    <div class="bg-yellow-300 border-4 border-yellow-500 rounded-lg p-8 shadow-lg text-center slide-in transform transition duration-300 hover:scale-105 hover:shadow-2xl">
        <h1 class="text-4xl font-bold mb-4 text-gray-800">Did You Know? 🤔</h1>
        <p class="text-lg text-gray-700">The shortest war in history lasted only 38 to 45 minutes! ⚔️</p>
    </div>
</body>
</html>
